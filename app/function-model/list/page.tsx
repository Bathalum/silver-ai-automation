import { redirect } from 'next/navigation'

export default function FunctionModelListRedirectPage() {
  // Redirect to the dashboard function model list
  redirect('/dashboard/function-model/list')
} 